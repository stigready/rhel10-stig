---
# StigForge supplements for RHEL 10 STIG — shared docker/SSG gap fills (see rhel9_stig supplement).

- name: Use files-only host resolution when DNS is not configured (STIG name resolution)
  ansible.builtin.lineinfile:
    path: /etc/nsswitch.conf
    regexp: '^hosts:'
    line: "hosts:      files myhostname"
  tags:
    - network_configure_name_resolution

- name: Empty resolv.conf when nsswitch does not use DNS
  ansible.builtin.copy:
    dest: /etc/resolv.conf
    content: ""
    owner: root
    group: root
    mode: "0644"
    force: true
  tags:
    - network_configure_name_resolution

- name: Find ungrouped files on local mounts (verify scope)
  ansible.builtin.shell: |
    set -euo pipefail
    mounts="$(findmnt -n -l -k -it "$(awk '/nodev/ { print $2 }' /proc/filesystems | paste -sd,)" -o TARGET 2>/dev/null || true)"
    for mp in $mounts; do
      find "$mp" -xdev -nogroup 2>/dev/null || true
    done
  register: stigforge_nogroup_files
  changed_when: false
  tags:
    - file_permissions_ungroupowned

- name: Assign root group to ungrouped files
  ansible.builtin.file:
    path: "{{ item }}"
    group: root
  loop: "{{ stigforge_nogroup_files.stdout_lines | select('match', '.+') | list }}"
  when: stigforge_nogroup_files.stdout_lines | length > 0
  tags:
    - file_permissions_ungroupowned
